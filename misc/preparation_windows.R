install.packages("shiny")
source('http://bioconductor.org/biocLite.R')
biocLite('phyloseq')
library(ggplot2)
install.packages("miLineage")

# put graphlan in working directory
# do: export PATH=`pwd`/graphlan/:$PATH

#download python 3.6.4(the newest in https://www.python.org/downloads/)

# do: sudo easy_install pip
# enter password
# do: sudo pip install biopython
# enter password

install.packages("png")

library(phyloseq)
library(miLineage)

# install python, check the option of "Add Python 3.6 to PATH"
# pip install biopython
# 